<div class="main_body card">
  <div class="event_post">
    <h4 class="event-title"><a
        class="link"
        href="/event/{{id}}"
      >{{event_title}}</a></h4>
    <div class="event-body">
      <p class="event-content">
        {{items.item_name}}
      </p>
      <p class="event-guest">
        Created by
        <span class="event-username">{{user.username}}</span>
        on
        {{format_date createdAt}}
      </p>
    </div>
  </div>
</div>